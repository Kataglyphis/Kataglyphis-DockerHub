{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "dependencyDashboard": true,
  "labels": ["dependencies"],
  "packageRules": [
    {
      "description": "GitHub Actions pin bumps are low-risk; group them into one PR.",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions"
    },
    {
      "description": "versions.env keys are notification-only: a bump requires the human ritual (sync_versions.py --write + refreshing the *_SHA256 pins per AGENTS.md § Version Bumping), so never auto-PR them.",
      "matchManagers": ["custom.regex"],
      "dependencyDashboardApproval": true
    },
    {
      "description": "Dockerfile digest updates (UBUNTU_DIGEST-style FROM pins) also need sync_versions.py --write to propagate; require dashboard approval.",
      "matchManagers": ["dockerfile"],
      "dependencyDashboardApproval": true
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "versions.env keys annotated with a renovate hint comment: # renovate: datasource=<ds> depName=<name> [extractVersion=...]",
      "managerFilePatterns": ["/^linux/scripts/01-core/versions\\.env$/"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>[a-z-.]+) depName=(?<depName>[^\\s]+)(?: extractVersion=(?<extractVersion>[^\\s]+))?\\s*(?:\\n# noforward)?\\n[A-Z0-9_]+=(?<currentValue>[^\\s#]+)"
      ]
    }
  ]
}
