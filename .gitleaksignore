# gitleaks false-positive allowlist — SUPERSEDED by .gitleaks.toml.
#
# Entries here are `file:rule:line` fingerprints, which stop matching as soon
# as an unrelated edit shifts the line; both triaged entries drifted that way
# and failed the preflight gate on main. Triaged false positives now live in
# .gitleaks.toml as value-pinned allowlist entries, which survive refactors.
#
# Kept (empty) so a stray fingerprint is not added here by habit — add the
# justification + regex to .gitleaks.toml instead.
