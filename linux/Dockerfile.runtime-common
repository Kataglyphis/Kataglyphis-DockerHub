# syntax=docker/dockerfile:1.24.0

# The canonical source for shared final-stage runtime elements (runtime
# scripts, entrypoint, WORKDIR, VOLUME, ENTRYPOINT, CMD, HEALTHCHECK,
# kataglyphis user, OCI labels) is now linux/Dockerfile.torch final stage.
#
# This file exists for documentation reference only.  It is NOT consumed
# by any build.  Do not add content here expecting it to appear in images.
# All final-image additions go in Dockerfile.torch's final stage.
