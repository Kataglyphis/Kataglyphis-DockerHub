# syntax=docker/dockerfile:1.24.0
#
# THIS FILE IS A DOCUMENTATION-ONLY REFERENCE.
# It is NOT consumed by any build pipeline.
# The canonical source for shared final-stage runtime elements is
# linux/Dockerfile.torch final stage.
#
# If this file contains active instructions, CI should fail —
# all final-image additions belong in Dockerfile.torch.
#
# To guard against accidental consumption, the FROM line below
# intentionally references a nonexistent image so any attempt to
# build this file will fail immediately.
FROM scratch
COPY DOES_NOT_EXIST / 

(End of file - total 9 lines)
