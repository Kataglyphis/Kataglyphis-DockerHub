# Copy to .env (same directory) and fill in real values. Never commit .env.
#
# Secret used by open-webui to sign sessions/JWTs. Generate one with e.g.:
#   openssl rand -base64 64 | tr -d '\n'
WEBUI_SECRET_KEY=change-me
