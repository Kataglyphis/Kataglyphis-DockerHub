diff --git a/tflite/tools/pip_package/build_pip_package_with_cmake.sh b/tflite/tools/pip_package/build_pip_package_with_cmake.sh
index 2723156..785d846 100755
--- a/tflite/tools/pip_package/build_pip_package_with_cmake.sh
+++ b/tflite/tools/pip_package/build_pip_package_with_cmake.sh
@@ -18,9 +18,9 @@ set -ex
 SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
 PYTHON="${CI_BUILD_PYTHON:-python3}"
 VERSION_SUFFIX=${VERSION_SUFFIX:-}
-export TENSORFLOW_DIR="${SCRIPT_DIR}/../../../.."
+export TENSORFLOW_DIR="${TENSORFLOW_DIR:-${SCRIPT_DIR}/../../..}"
 TENSORFLOW_LITE_DIR="${TENSORFLOW_DIR}/tensorflow/lite"
-TENSORFLOW_VERSION=$(grep "TF_VERSION = " "${TENSORFLOW_DIR}/tensorflow/tf_version.bzl" | cut -d= -f2 | sed 's/[ "-]//g')
+TENSORFLOW_VERSION="${TENSORFLOW_VERSION:-$(grep "TF_VERSION = " "${TENSORFLOW_DIR}/tensorflow/tf_version.bzl" 2>/dev/null | cut -d= -f2 | sed 's/[ "-]//g')}"
 IFS='.' read -r -a array <<< "$TENSORFLOW_VERSION"
 TF_MAJOR=${array[0]}
 TF_MINOR=${array[1]}
@@ -74,7 +74,7 @@ if [[ "${TENSORFLOW_TARGET}" != "native" ]]; then
   HOST_BUILD_DIR="${BUILD_DIR}/cmake_build_host"
   mkdir -p "${HOST_BUILD_DIR}"
   pushd "${HOST_BUILD_DIR}"
-  cmake "${TENSORFLOW_LITE_DIR}"
+  cmake ${EXTRA_CMAKE_FLAGS:-} "${TENSORFLOW_LITE_DIR}"
   cmake --build . --verbose -j ${BUILD_NUM_JOBS} -t flatbuffers-flatc
   popd
 fi
@@ -88,7 +88,7 @@ case "${TENSORFLOW_TARGET}" in
   armhf_vfpv3)
     eval $(${TENSORFLOW_LITE_DIR}/tools/cmake/download_toolchains.sh "${TENSORFLOW_TARGET}")
     ARMCC_FLAGS="${ARMCC_FLAGS} ${TF_CXX_FLAGS} -I${PYBIND11_INCLUDE} -I${NUMPY_INCLUDE}"
-    cmake \
+    cmake ${EXTRA_CMAKE_FLAGS:-} \
       -DCMAKE_C_COMPILER=${ARMCC_PREFIX}gcc \
       -DCMAKE_CXX_COMPILER=${ARMCC_PREFIX}g++ \
       -DCMAKE_C_FLAGS="${ARMCC_FLAGS}" \
@@ -102,7 +102,7 @@ case "${TENSORFLOW_TARGET}" in
   armhf)
     eval $(${TENSORFLOW_LITE_DIR}/tools/cmake/download_toolchains.sh "${TENSORFLOW_TARGET}")
     ARMCC_FLAGS="${ARMCC_FLAGS} ${TF_CXX_FLAGS} -I${PYBIND11_INCLUDE} -I${NUMPY_INCLUDE}"
-    cmake \
+    cmake ${EXTRA_CMAKE_FLAGS:-} \
       -DCMAKE_C_COMPILER=${ARMCC_PREFIX}gcc \
       -DCMAKE_CXX_COMPILER=${ARMCC_PREFIX}g++ \
       -DCMAKE_C_FLAGS="${ARMCC_FLAGS}" \
@@ -116,7 +116,7 @@ case "${TENSORFLOW_TARGET}" in
   rpi0)
     eval $(${TENSORFLOW_LITE_DIR}/tools/cmake/download_toolchains.sh "${TENSORFLOW_TARGET}")
     ARMCC_FLAGS="${ARMCC_FLAGS} ${TF_CXX_FLAGS} -I${PYBIND11_INCLUDE} -I${NUMPY_INCLUDE}"
-    cmake \
+    cmake ${EXTRA_CMAKE_FLAGS:-} \
       -DCMAKE_C_COMPILER=${ARMCC_PREFIX}gcc \
       -DCMAKE_CXX_COMPILER=${ARMCC_PREFIX}g++ \
       -DCMAKE_C_FLAGS="${ARMCC_FLAGS}" \
@@ -130,7 +130,7 @@ case "${TENSORFLOW_TARGET}" in
   aarch64)
     eval $(${TENSORFLOW_LITE_DIR}/tools/cmake/download_toolchains.sh "${TENSORFLOW_TARGET}")
     ARMCC_FLAGS="${ARMCC_FLAGS} ${TF_CXX_FLAGS} -I${PYBIND11_INCLUDE} -I${NUMPY_INCLUDE}"
-    cmake \
+    cmake ${EXTRA_CMAKE_FLAGS:-} \
       -DCMAKE_C_COMPILER=${ARMCC_PREFIX}gcc \
       -DCMAKE_CXX_COMPILER=${ARMCC_PREFIX}g++ \
       -DCMAKE_C_FLAGS="${ARMCC_FLAGS}" \
@@ -142,15 +142,15 @@ case "${TENSORFLOW_TARGET}" in
       "${TENSORFLOW_LITE_DIR}"
     ;;
   native)
-    BUILD_FLAGS=${BUILD_FLAGS:-"-march=native ${TF_CXX_FLAGS} -I${PYTHON_INCLUDE} -I${PYBIND11_INCLUDE} -I${NUMPY_INCLUDE}"}
-    cmake \
+    BUILD_FLAGS=${BUILD_FLAGS:-"-idirafter /usr/include ${TF_CXX_FLAGS} -I${PYTHON_INCLUDE} -I${PYBIND11_INCLUDE} -I${NUMPY_INCLUDE}"}
+    cmake ${EXTRA_CMAKE_FLAGS:-} \
       -DCMAKE_C_FLAGS="${BUILD_FLAGS}" \
       -DCMAKE_CXX_FLAGS="${BUILD_FLAGS}" \
       "${TENSORFLOW_LITE_DIR}"
     ;;
   *)
     BUILD_FLAGS=${BUILD_FLAGS:-"${TF_CXX_FLAGS} -I${PYTHON_INCLUDE} -I${PYBIND11_INCLUDE} -I${NUMPY_INCLUDE}"}
-    cmake \
+    cmake ${EXTRA_CMAKE_FLAGS:-} \
       -DCMAKE_C_FLAGS="${BUILD_FLAGS}" \
       -DCMAKE_CXX_FLAGS="${BUILD_FLAGS}" \
       "${TENSORFLOW_LITE_DIR}"
