--- a/modules/videoio/src/cap_ffmpeg_hw.hpp	2026-08-21 14:09:30.030215407 +0200
+++ b/modules/videoio/src/cap_ffmpeg_hw.hpp	2026-08-21 14:09:30.046330279 +0200
@@ -757,9 +757,21 @@
 #endif
             if (hw_type == AV_HWDEVICE_TYPE_CUDA) // CUDA encoders don't support avcodec_get_hw_config()
                 hw_native_fmt = AV_PIX_FMT_CUDA;
-            if (av_codec_is_encoder(c) && hw_native_fmt != AV_PIX_FMT_NONE && c->pix_fmts) {
-                for (int i = 0; c->pix_fmts[i] != AV_PIX_FMT_NONE; i++) {
-                    if (c->pix_fmts[i] == hw_native_fmt) {
+#if LIBAVCODEC_VERSION_MAJOR >= 62
+            // FFmpeg 8+: AVCodec.pix_fmts was removed; query via
+            // avcodec_get_supported_config() (returns the same terminated array).
+            const enum AVPixelFormat* hw_codec_pix_fmts = NULL;
+            {
+                const void* hw_cfg_out = NULL; int hw_cfg_n = 0;
+                if (avcodec_get_supported_config(NULL, c, AV_CODEC_CONFIG_PIX_FORMAT, 0, &hw_cfg_out, &hw_cfg_n) >= 0)
+                    hw_codec_pix_fmts = (const enum AVPixelFormat*)hw_cfg_out;
+            }
+#else
+            const enum AVPixelFormat* hw_codec_pix_fmts = c->pix_fmts;
+#endif
+            if (av_codec_is_encoder(c) && hw_native_fmt != AV_PIX_FMT_NONE && hw_codec_pix_fmts) {
+                for (int i = 0; hw_codec_pix_fmts[i] != AV_PIX_FMT_NONE; i++) {
+                    if (hw_codec_pix_fmts[i] == hw_native_fmt) {
                         *hw_pix_fmt = hw_native_fmt;
                         if (hw_check_codec(c, hw_type, disabled_codecs))
                             return c;
--- a/modules/videoio/src/cap_ffmpeg_impl.hpp	2026-08-21 14:09:30.032358032 +0200
+++ b/modules/videoio/src/cap_ffmpeg_impl.hpp	2026-08-21 14:09:30.046654248 +0200
@@ -2629,8 +2629,20 @@
     c->time_base.den = frame_rate;
     c->time_base.num = frame_rate_base;
     /* adjust time base for supported framerates */
-    if(codec && codec->supported_framerates){
-        const AVRational *p= codec->supported_framerates;
+#if LIBAVCODEC_VERSION_MAJOR >= 62
+    // FFmpeg 8+: AVCodec.supported_framerates was removed; query via
+    // avcodec_get_supported_config() (same {0,0}-terminated array semantics).
+    const AVRational* cv_supported_framerates = NULL;
+    if (codec) {
+        const void* fr_cfg_out = NULL; int fr_cfg_n = 0;
+        if (avcodec_get_supported_config(NULL, codec, AV_CODEC_CONFIG_FRAME_RATE, 0, &fr_cfg_out, &fr_cfg_n) >= 0)
+            cv_supported_framerates = (const AVRational*)fr_cfg_out;
+    }
+#else
+    const AVRational* cv_supported_framerates = codec ? codec->supported_framerates : NULL;
+#endif
+    if(codec && cv_supported_framerates){
+        const AVRational *p= cv_supported_framerates;
         AVRational req = {frame_rate, frame_rate_base};
         const AVRational *best=NULL;
         AVRational best_error= {INT_MAX, 1};
